(function () { const attacks = [ { name: "Ataque Fraco", damage: 10, cooldown: "2s", icon: "🥊" }, { name: "Ataque Médio", damage: 20, cooldown: "5s", icon: "🗡️" }, ]; function createDialog() { const dialog = document.createElement("dialog"); dialog.style.padding = "20px"; dialog.style.borderRadius = "10px"; dialog.style.border = "1px solid #ccc"; dialog.style.maxWidth = "400px"; dialog.style.fontFamily = "Arial, sans-serif"; dialog.innerHTML = ` <h2>Escolha seu ataque</h2> <table style="width: 100%; text-align: center; border-collapse: collapse;"> <thead> <tr> <th>Ícone</th> <th>Nome</th> <th>Dano</th> <th>Cooldown</th> </tr> </thead> <tbody id="attackTableBody"> ${attacks.map((atk, index) => ` <tr data-index="${index}" style="cursor: pointer;"> <td>${atk.icon}</td> <td>${atk.name}</td> <td>${atk.damage}</td> <td>${atk.cooldown}</td> </tr> `).join("")} </tbody> </table> `; document.body.appendChild(dialog); return dialog; } function bindEventHandlers() { const rows = document.querySelectorAll("#attackTableBody tr"); rows.forEach(row => { row.addEventListener("click", () => { const index = parseInt(row.getAttribute("data-index")); const atk = attacks[index]; alert(`Você escolheu: ${atk.name} (${atk.icon})!`); }); }); } function startAutoClick() { const firstRow = document.querySelector("#attackTableBody tr"); if (firstRow) { firstRow.click(); } else { console.warn("Nenhuma"); } } const dialog = createDialog(); dialog.showModal(); bindEventHandlers(); setTimeout(startAutoClick, 500); })();
